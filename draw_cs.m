%general definitions
%tip of the arrow
xar = 0.5*x_t_max;

%color of the CS
color_cs = 'k';

%we have to print the letter 'x' while displaying the CS
%the letter x is stored as a string
display_cs = 'x';

%the offset of the CS in the Y direction
y_csoffset = 1.0;

%plotting the CS
%plotcs(axis_ani,xar,display_cs,y_csoffset,color_cs,linewidth,fontsize)