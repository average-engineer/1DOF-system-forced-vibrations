%coordinates of the excitation arrow
max_length_arrow = 0.5*x_t_max;

%color of the arrow
clr_a = 'k';

%y offset distance of the arrow
ya_offset = 0.0;

%arrow_head = [pos_m(1)-dim_m(1),0,0];

%arrow_foot = [arrow_head(1) - max_length_arrow,0,0];

%plotting the arrow
%plotarrow(axis_ani,arrow_head,arrow_foot,ya_offset,clr_a)