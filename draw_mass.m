%dimesnsions of the mass 
%dimension = [x dim, y dim, z dim]
dim_m = [0.75*x_t_max, 1, 1];

%position of the central axis/coordinate system the mass cuboid
%pos_m = [abs(min(x_t))-0.02,0,0];
%color of the mass
clr_m = 'r';

%plotting the mass
%using a function to plot a cube representing the mass
%plotcube(axis_ani,dim_m,pos_m,clr_m);